# First session

The file [./apps/topwork/sessions/session00/session.md] contains a sequence of events and references to screenshots of an existing web application. Your job is to observe these events and screenshots, using them as a guide to implementing this UI. Your implementation goes in ./apps/topwork/, and should follow the patterns seen in the [./apps/todo/] example. Ensure that all components of the original app are implemented.

As you work, capture the current state of the implementation in [./apps/topwork/sitemap.md], broken down by page / path. Be precise about what user-visible features are implemented, and anything that's currently a placeholder (e.g., an unimplemented form, links that go nowhere yet, etc). This sitemap will be used to consider subsequent design examples, and decide what needs to be implemented to move the implementation forward.

Also Include the data types used in the current implementation. Do not add any notes about speculative features; focus solely on the existing implementation.

# Additional sessions

There's a new session in ./apps/topwork/sessions/session01, that I'd like you to take into account in the design. Examine the session's events and screenshots, compare them with our existing implementation, and create a detailed plan for any changes or additions we will need to implement these behaviors, if any.
