---
"subtext": patch
---

Vendor the sightmap skills from `@sightmap/sightmap@0.32.0` (was `0.27.0`). Refreshes `sightmap-authoring` and `sightmap-browser` to the latest published versions and pins the source exactly.
